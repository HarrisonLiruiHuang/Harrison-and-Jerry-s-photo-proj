services:
  - type: web
    name: fivek-editing-suggestions
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
